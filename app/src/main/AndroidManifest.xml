<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_main_app"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_main_app"
        android:supportsRtl="true"
        android:theme="@style/Theme.TechTitans"
        tools:targetApi="31">
        <activity
            android:name=".auth.GameRulesActivity"
            android:exported="false" />
        <activity
            android:name=".content_activities.AII"
            android:exported="false" />
        <activity
            android:name=".content_activities.BotsActivity"
            android:exported="false" />
        <activity
            android:name=".content_activities.ChatActivity"
            android:exported="false" />
        <activity
            android:name=".content_activities.ConversationActivity"
            android:exported="false" />
        <activity
            android:name=".auth.AuthActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".auth.RegistrationActivity"
            android:exported="false" />
    </application>

</manifest>